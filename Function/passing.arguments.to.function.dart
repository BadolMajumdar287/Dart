//PASSING ARGUMENTS TO FUNCTIONS
//When a function is called,it may have some information as per the function protype is known as a parameter(arguments).

void main() {
  multipleNmber(int x, int y) {
    print(x * y);
  }

  multipleNmber(10, 10);
  multipleNmber(10, 12);
  multipleNmber(20, 20);
}
