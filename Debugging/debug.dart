void main(List<String> args) {
  var x = 20;
  var y = 10;

  var add = x + y;
  var sub = x - y;
  var mul = x * y;
  var div = x / y;
  var mod = x % y;

  print(add);
  print(sub);
  print(mul);
  print(div);
  print(mod);
}
